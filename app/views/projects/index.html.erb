<div class="container">
<p id="notice"><%= notice %></p>

<h1>Projects</h1>

<table>
  <thead>
    <tr>
      <th>Project name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= link_to project.project_name, project %></td>
        <td><%= link_to 'Mofifier', edit_project_path(project), :class => "btn btn-warning", :style => "color: white" %></td>
        <td><%= link_to 'Détruire', project, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger", :style => "color: white" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '+ Ajouter un nouveau projet', new_project_path, :class => "btn btn-primary", :style => "color: white" %>
</div>